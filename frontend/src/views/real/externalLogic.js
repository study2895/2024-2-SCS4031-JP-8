// externalLogic.js

export function processStationData(stationId, idx, data) {
  // stationId와 idx 값을 활용하여 추가 로직을 구현
  console.log(`Station ID: ${stationId}, Index: ${idx}, Data:`, data)

  // 여기에서 data를 활용하여 원하는 작업 수행
  // 예: 특정 데이터 분석, API 호출, 데이터 가공 등
}
